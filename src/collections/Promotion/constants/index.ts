export const DISCOUNT_APPLY_SCOPE = {
  totalOrderValue: {
    label: 'Total order value',
    value: 'total_order_value',
  },
  perOrderItem: {
    label: 'Each item in the order',
    value: 'per_order_item',
  },
}

export const DISCOUNT_APPLY_SCOPES = Object.values(DISCOUNT_APPLY_SCOPE)
